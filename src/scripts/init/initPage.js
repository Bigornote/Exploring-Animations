/**
 * initPage()
 * ------------------------------------------------------------
 * Exécuté à CHAQUE entrée de page :
 * - au chargement initial
 * - après chaque content:replace de Swup
 *
 * On y place :
 * - effets GSAP/ScrollTrigger
 * - modules dépendants du DOM courant
 * - comportements liés aux éléments HTML de la page
 *
 * ❌ Ne jamais mettre ici :
 * - smooth scroll (Lenis)
 * - listeners globaux permanents
 *
 * 💡 Règle simple :
 * "Si ça doit être reset/reinit à chaque transition —> initPage()"
 */


export function initPage() {
  // initModules();
}